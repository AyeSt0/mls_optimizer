# Contributing to mls_optimizer
- Use feature branches: `feat/...`, `fix/...`, `chore/...`
- Write clear commit messages (Conventional Commits recommended)
- Run the GUI locally: `python scripts/00_launch_gui.py`
- Keep scripts cross-platform (Windows/PowerShell + Unix shells)
